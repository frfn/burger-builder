import React from "react";
// import { render } from "@testing-library/react";
// import App from "../App/App";
// import { Switch, Route } from "react-router-dom";

// import { configure, shallow } from "enzyme";
// import Adapter from "enzyme-adapter-react-16";
// import Layout from "../../Components/Layout/Layout";

// test('renders learn react link', () => {
//   const { getByText } = render(<App />);
//   const linkElement = getByText(/learn react/i);
//   expect(linkElement).toBeInTheDocument();
// });

// configure({ adapter: new Adapter() });

// describe("<App />", () => {
// 	let wrapper;
// 	beforeEach(() => {
// 		wrapper = shallow(<App />);
// 	});

// 	/* i wanna test that it will have 3 route components... i just dont know how to do that yet */
// 	it("should have 3 <Route /> available if NOT authenticated", () => {
// 		wrapper.setProps({ isAuth: false });
// 		expect(
// 			wrapper.find(Layout).find("routes").find(Switch).find(Route)
// 		).toHaveLength(3);
// 	});
// });
